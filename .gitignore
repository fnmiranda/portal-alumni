node_modules/
.env
dist/
.DS_Store

# Logs e Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Prisma (Caso gere arquivos locais de teste)
/server/prisma/*.db
/server/prisma/*.db-journal

# Pasta de Build do React (Geralmente é 'build' ou 'dist')
/client/build/
/client/dist/

# Se você decidir não versionar o lock para forçar a instalação limpa na sprint
# package-lock.json (Opcional, mas ajuda se a equipe for iniciante)
